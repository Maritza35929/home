<%= form_with(model: tecnico, local: true) do |form| %>
  <% if tecnico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tecnico.errors.count, "error") %> prohibited this tecnico from being saved:</h2>

      <ul>
        <% tecnico.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.number_field :telefono %>
  </div>

  <div class="field">
    <%= form.label :profesion_u_oficio %>
    <%= form.text_field :profesion_u_oficio %>
  </div>

  <div class="field">
    <%= form.label :servicios_ofrecidos %>
    <%= form.text_area :servicios_ofrecidos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
